pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Heliodos"
include ':app'
include ':colorpicker'
project(':colorpicker').projectDir = new File(rootDir, 'colorpicker/colorpicker')

includeBuild('astronomy/source/kotlin') {
    dependencySubstitution {
        substitute module('io.github.cosinekitty:astronomy') using project(':')
    }
}
